#!/bin/sh

# Configure node for distributed Erlang with IPv4 support
export ERL_AFLAGS="-proto_dist inet_tcp"
export ECTO_IPV6="false"
# export DNS_CLUSTER_QUERY="myapp.internal" # Adjust if necessary for local DNS simulation
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="myapp@127.0.0.1"
